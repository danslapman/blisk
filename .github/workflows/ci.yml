name: CI

on: [push, pull_request]

jobs:
  test:
    name: Run tests
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: blisk
            asset_name: blisk-linux-amd64
          - os: windows-latest
            artifact_name: blisk.exe
            asset_name: blisk-windows-amd64.exe
          - os: macos-15-intel
            artifact_name: blisk
            asset_name: blisk-macos-amd64
          - os: macos-latest
            artifact_name: blisk
            asset_name: blisk-macos-arm
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cargo test
      - name: Build release executable
        run: cargo build --release
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
      - name: Publish binaries
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/release/${{ matrix.artifact_name }}
          asset_name: ${{ matrix.asset_name }}
          tag: ${{ github.ref }}
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
